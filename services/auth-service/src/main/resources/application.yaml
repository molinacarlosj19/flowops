server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/flowops_auth
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: auth-service